下面这 4 件事补齐后，就可以直接开跑：

train.py 增加 route=vis 分支

选择视觉专用模型（2 维输出），比如我们前面给你的 VisionVarNet；

loss 用 Gaussian NLL 或 Student-t（仅对 mask_vis==1 计损失）。

dataset.py 支持视觉键

从 npz 读 X_vis (T,D) / E_vis (T,2) / mask_vis (T,2)；

若同目录存在 vis_meta.json 就用其中的 mean/std 做标准化；否则只在 train 上统计，val/test 冻结。

gen_bicycle_dual_vis.py 落盘元数据

写出 data/synth_vis/vis_meta.json（单位=px、feature_names、standardize 的 mean/std、seed 与段落配额）；

写出 seg_id_{train,val,test}.npy 或把 seg_id 内嵌到各自 npz。
（README 里目前看不到这两样，建议纳入版本控制。）
GitHub

eval.py / analyze.py 加 route=vis

评测时计算视觉 2 维的 E[z²]、cov68/95；

画三图：z² 直方图（df=2）、err² vs var 散点、logvar 时序。